import React from 'react';
import { View, Text } from 'react-native';
import postModal from "@/app/modals/postModal";
import PostModal from "@/app/modals/postModal";
const newpost = () => {
    return (
       <PostModal />
    );
};

export default newpost;
